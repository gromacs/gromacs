# If data files are to be provided with the gmx package, standard
# distutils functionality is provided with ``include_package_data``.
# If data files are just for testing, we can be sloppier and use
# os.path tools with the ``__file__`` attribute to get relative paths.
