# - Config file for the TNG_IO package

include(tng_io.cmake)

get_target_property(_libs tng_io::tng_io INTERFACE_LINK_LIBRARIES)
if (_libs MATCHES "ZLIB::")
    include(CMakeFindDependencyMacro)
    find_dependency(ZLIB)
endif()
unset(_libs)
