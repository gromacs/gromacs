FROM centos:7
RUN yum update -y && \
    yum install -y epel-release && \ 
    yum install -y \
        cmake3 \
        CUnit-devel \
        ctags \
        gcc \
        gcc-c++ \
        fftw-devel \
        libzstd-devel \
        netcdf-devel \
        make \
        swig3 \
        python-devel \
        numpy \
        zlib-devel && \
    yum clean all
COPY . /build/
WORKDIR /build/
RUN rm -rf build && \
    mkdir -p build && \
    cd build && \
    cmake3 .. && \
    make -j 
